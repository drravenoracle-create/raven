import type { ReactNode } from "react";
export const metadata = { title: "Raven Blackwood | Classical Divination", description: "A calm guide for turning uncertainty into your next step." };
export default function EnglishLayout({ children }: { children: ReactNode }) { return <div lang="en">{children}</div>; }
