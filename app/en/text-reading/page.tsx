"use client";
import { useState } from "react";
export default function EnglishTextReading(){const [text,setText]=useState("");const [result,setResult]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const r=await fetch("/api/raven?return_to=/en/text-reading/",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({mode:"reading",readingMode:"consultation",readingModeLabel:"Consultation",divination:"integrated",divinationLabel:"Integrated reading",sourceText:text,locale:"en"})});const p=await r.json();setResult(p.text||p.error||"A reading could not be generated.")}finally{setBusy(false)}}return <main className="raven-page min-h-screen px-5 py-10"><div className="mx-auto max-w-3xl"><a href="/en/">← Raven Blackwood</a><h1 className="mt-8 text-4xl font-semibold">English AI text reading</h1><p className="mt-4 leading-8 text-[#5e625c]">Paste a message, draft reply, or consultation in Japanese or English. The result will be written in English.</p><form onSubmit={submit} className="raven-card mt-8 p-6"><textarea className="min-h-56 w-full rounded border p-3" value={text} onChange={e=>setText(e.target.value)} placeholder="Paste your message or consultation here." required/><button className="raven-hero-button mt-4" disabled={busy}>{busy?"Reading…":"Get an English reading"}</button></form>{result&&<article className="raven-card mt-6 whitespace-pre-wrap p-6 leading-8">{result}</article>}</div></main>}
