"use client";
import { useState } from "react";
export default function EnglishFreeFortune(){const [concern,setConcern]=useState("");const [result,setResult]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{const r=await fetch("/api/raven",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({mode:"fortune",theme:"today",concern,locale:"en"})});const p=await r.json();setResult(p.reading?`${p.reading.title}\n\n${p.reading.summary}\n\nAdvice\n${p.reading.advice}\n\nCaution\n${p.reading.caution}`:p.error||"A reading could not be generated.")}finally{setBusy(false)}}return <main className="raven-page min-h-screen px-5 py-10"><div className="mx-auto max-w-3xl"><a href="/en/">← Raven Blackwood</a><h1 className="mt-8 text-4xl font-semibold">Free reading</h1><p className="mt-4 leading-8 text-[#5e625c]">Share a question and receive a short reflective reading.</p><form onSubmit={submit} className="raven-card mt-8 p-6"><textarea className="min-h-40 w-full rounded border p-3" value={concern} onChange={e=>setConcern(e.target.value)} placeholder="What would you like to consider?" required/><button className="raven-hero-button mt-4" disabled={busy}>{busy?"Reading…":"Start free reading"}</button></form>{result&&<article className="raven-card mt-6 whitespace-pre-wrap p-6 leading-8">{result}</article>}</div></main>}
